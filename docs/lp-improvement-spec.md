# /lp 改善 実装指示書（体験予約率の改善）

対象: `~/natural fitness`（repo: personalsol0514-art/lp）の `/lp` ページ。
目的: 体験予約数の増加。特にスマホ。世界観は「清潔・安心・自然体・人の温度・岡崎の地域密着」。
禁止: 女性専用に見える表現／手ぶらOKと誤解される表現／「理想の身体へ」系の抽象コピー。

**2026-07-02 方針決定（ユーザー確認済み）**
- LINEボタンは現状維持（実LINEアカウント未確認のため削除しない。将来URLが判明したら差し替え）。
- 実店舗写真の撮影は今回のスコープ外。public/ の既存画像を流用して実装する。

**2026-07-03 FVキャッチコピー確定（ユーザー確認済み）**
- 案A（自然体・継続重視系）に決定。
- キャッチ: `無理なく、自分のペースで。`
- サブ: `ダイエット・姿勢改善・運動習慣づくりを、\n続けやすいペースでマンツーマンサポート。`

## 変更0（全体ルール）

- 「女性の」「女性らしい」「女性ホルモン」等の性別限定表現を全ファイルから除去し、「はじめての方」「運動が苦手な方」「一人ひとりの体力に合わせて」に置換。
- LINEボタンは削除しない。既存の `/reserve` リンクのまま維持。
- CTA文言は次の3種に統一: 主=「初回体験を予約する」/ 低摩擦=「体験の空き時間を見てみる」/ 料金内=「0円で体験を予約する」。すべて `/reserve` へ。ReserveLink の eventLabel は既存命名に倣い付与。

## 変更1: components/Hero.tsx（FV全面改修）

1. スライドショー廃止。`hero-gym.jpg` 1枚固定（`priority`）。useState/useEffect/interval を削除し Server Component 化。
2. 背景 `bg-[#1d1b2e]` → 写真の上に `bg-gradient-to-t from-black/55 via-black/25 to-transparent` のオーバーレイ。テキストは左寄せ・左下配置に変更（現在の右寄せをやめる）。
3. コンテンツ構成（上から）:
   - 小ラベル: `岡崎市本町通・東岡崎エリア｜完全個室パーソナルジム`（text-xs〜sm、白、tracking広め）
   - キャッチ（p要素・視覚上の主見出し）: `無理なく、自分のペースで。`（現行と同等のclamp サイズ）
   - SEO用 h1（sr-only）: `岡崎市本町通の完全個室パーソナルジム NATURAL FITNESS`
   - サブコピー: `ダイエット・姿勢改善・運動習慣づくりを、\n続けやすいペースでマンツーマンサポート。`
   - CTAボタン: `初回体験を予約する`（bg-[#E07A3A]、rounded-full、min-h-[52px]、全幅 max-w-[420px]）
   - CTA直下マイクロコピー（text-xs 白/85）: `初回0円（先着5名）・約60分・無理な勧誘はしません`
   - 信頼チップ3つ横並び（白/15背景+白文字の小さなpill）: `完全個室` `駐車券サービス` `初心者歓迎`
4. セクション高さ `min-h-screen` → `min-h-[92svh]`。
5. 帯「スキマ時間でできる健康習慣」は削除。

## 変更2: components/MobileCTA.tsx（追従ボタン）

- 現行の2分割構成（体験予約／LINE相談）は維持。LINEボタンは削除しない。
- "use client" 化し IntersectionObserver で `#top`（FV）が画面内にある間は非表示（FVに自前CTAがあるため二重表示を避ける）。

## 変更3: components/Header.tsx

- navItems に `体験の流れ`(`#flow`) を追加（料金の前）。
- メニュー内ボタン文言 `体験予約` → `初回体験を予約する`。

## 変更4: components/Solution.tsx（性別中立化）

- pillar "movement" の desc: `激しいトレーニングは必要ありません。\n\nその人の体力と目的に合わせたやさしい動きで、\n日常のクセを整えていくことで、\n自然と引き締まっていきます。`
- 他pillarも「女性」表現があれば同様に除去。

## 変更5: components/Reasons.tsx（理由3→4、中間CTA）

- reason[1]（女性の身体に〜）を差し替え:
  - accentLabel: `一人ひとりに合わせて`
  - title: `体調と体力に合わせるオーダーメイド`
  - desc: `年齢も体力も目標も、人によって違います。その日の体調・生活リズム・過去のケガまで確認しながら、メニューをその場で調整。無理な食事制限はせず、続けられる形で整えていきます。`
- reason追加（4つ目）:
  - accentLabel: `岡崎で通いやすい`
  - title: `完全個室×駐車券サービス`
  - desc: `人目を気にせず集中できる完全個室。お車の方にはタカラパーキングのサービス券をお渡しします。営業は20時まで、お仕事帰りにも通いやすい岡崎市本町通のジムです。`
  - imageSrc: `/gallery-studio-interior.png`
- グリッドを sm:grid-cols-2 lg:grid-cols-4 に（4項目対応）。スマホの `w-[70%]` 制約は `w-full` に変更（可読性）。
- セクション末に中間CTA: テキストリンク型 `→ 体験の空き時間を見てみる`（/reserve、text-[#c45a28] underline）。

## 変更6: components/BeforeAfter.tsx

- 2枚目カードの注記 `※別シチュエーションでのビフォー例です` / `※トレーニングを重ねたあとのフォーム例のイメージです` を実文に差し替え。実例が用意できない場合は2枚目カードごと削除し1枚構成に。
- セクション末尾に `※効果には個人差があります` を追加。

## 変更7: components/Trainer.tsx（増量）

- 名前の下に: 指導歴・資格・得意分野の3行リスト（値はプレースホルダーで良い。例: `指導歴6年／栄養コンシェルジュ／姿勢改善・ボディメイク`。実値は店舗確認）。
- 挨拶文の「忙しくて自分のことを後回しにしてきた女性が」→「忙しくて自分のことを後回しにしてきた方が」。
- 末尾にトレーナーの言葉として blockquote: `「体験に来たからといって、入会を迫ることはありません。まずは今の体の状態を一緒に確認しましょう。」`

## 変更8: components/Studio.tsx

- 見出し `Gallery` → `店内の様子`（日本語メイン、英字 STUDIO は小ラベルに降格）。
- 可能なら StudioGalleryMarquee の各写真にキャプション（完全個室のトレーニングルーム／入口／カウンセリングスペース）。

## 変更9: components/Pricing.tsx（CTA設置・見せ方）

1. 初回体験カード内、価格の直下に CTAボタン `0円で体験を予約する`（ReserveLink、eventLabel="pricing_trial_reserve"、全幅、bg-[#E07A3A]）。
2. MonthlyPlanGrid: 「1回あたり◯円」を上（大きく font-bold text-slate-900 text-lg）、月額合計を下（小さく）に反転。
3. 月額プランの下に注記リスト:
   - `回数の縛り・違約金はありません`
   - `入会金0円`
   - `体験だけのご利用もOKです`
4. セクション末に低摩擦CTA: `→ 体験の空き時間を見てみる`。

## 変更10: 新規 components/TrialFlow.tsx（体験の流れ・新設）

- `id="flow"`、料金セクションの直後・FAQの前に配置（app/lp/page.tsx に追加）。
- 見出し: `初回体験の流れ`（h2）＋サブ `所要約60分。その場で入会を決める必要はありません。`
- 4ステップカード（スマホ縦積み・sm以上横並び）:
  1. `WEBで予約` — 空き時間から選ぶだけ。24時間受付。
  2. `カウンセリング` — お悩み・目標・体の状態を確認。
  3. `体験トレーニング` — 今の体力に合わせた内容で。きつくしません。
  4. `プランのご説明` — 合いそうなら続け方をご提案。**勧誘はしません。**
- 持ち物注記: `動きやすい服装・室内シューズ・タオル・飲み物をお持ちください（レンタルはありません）`
- 直下にCTAボタン `初回体験を予約する`。
- 画像は public/trial-*.png（reservation-phone / counseling / posture-check / plan-proposal）を流用可。

## 変更11: components/FAQ.tsx（再編）

faqs 配列を以下9問に差し替え（回答は上記本文セクション5のFAQ案の通り）:
1. 運動初心者・運動が苦手でも大丈夫ですか？（既存維持）
2. 男性も通えますか？ → `はい。女性専用ではありません。20〜50代の男女に通っていただいています。`
3. 駐車場はありますか？ → `タカラパーキングをご利用ください。駐車券のサービス券をお渡しします。`
4. 持ち物はありますか？ → `動きやすい服装・室内シューズ・タオル・飲み物をお持ちください。ウェアやシューズのレンタル（手ぶらプラン）はありません。`
5. 無理な勧誘はありませんか？ → `ありません。体験後にその場で入会を迫ることはありません。ご自宅でゆっくりご検討ください。`
6. 体験当日は何をしますか？ → `カウンセリング→姿勢チェック→体験トレーニング→プランのご説明で約60分です。`
7. 週に何回くらい通えばよいですか？（既存維持）
8. ダイエットだけを目標にしても通えますか？（既存維持）
9. 筋肉がつきすぎたりしませんか？ → 既存回答から「女性のホルモンバランスでは」等を除去し中立化。
- 見出し `パーソナルジムでよくある質問集` → `体験前によくいただく質問`。
- FAQPage JSON-LD を app/lp/page.tsx に追加（faq配列から生成）。

## 変更12: components/FinalCTA.tsx

- `LINEで相談` ボタンは削除しない（現状維持）。
- 主ボタン文言 `体験予約` → `初回体験を予約する`。
- 営業時間行に `（お仕事帰りにも通えます）` を追記。

## 変更13: app/lp/page.tsx（メタ・構造）

- title: `岡崎のパーソナルジム体験なら NATURAL FITNESS｜初回0円・完全個室（岡崎市本町通）`
- description: `岡崎市本町通の完全個室パーソナルジム。ダイエット・姿勢改善・運動習慣づくりを無理なく続くペースでサポート。初回体験0円（先着5名）、駐車券サービスあり、営業20時まで。無理な勧誘はしません。`
  ※「女性向け」を必ず削除。
- LocalBusiness JSON-LD を拡張: `address`（PostalAddress 完全住所 〒444-0051 愛知県岡崎市本町通2丁目3 鳥居ビル1F）、`telephone`、`openingHours: Mo-Su 11:00-20:00`、`priceRange: ¥0-¥60,000`、`geo`（35.9046, 137.1548 付近※要確認）。
- セクション順: Hero → Worries → Solution → Reasons → BeforeAfter → Trainer → Studio → Pricing → **TrialFlow（新規）** → FAQ → FinalCTA → MobileCTA。
- sr-only h2 の文言にキーワード: 例 `岡崎市で初心者に選ばれる理由`。

## 変更14: 検証とデプロイ

1. `npx tsc --noEmit` → `npm run build` 成功を確認。
2. `next dev`（port 3100）+ 375px幅で確認: FV内にCTAが収まる／追従ボタンがFVで消える／料金→流れ→FAQの導線。
3. **重要**: dev サーバー稼働中に `npm run build` を実行しない（.next が壊れる。壊れたら `rm -rf .next`）。
4. `git add`（app/lp, components, docs は除外可, out/ 含む）→ commit → push（Cloudflare 自動デプロイ）。

## 保留・要店舗確認

- トレーナーの実際の指導歴・資格（プレースホルダー実値化）
- 実LINEアカウントのURL（判明次第、MobileCTA/FinalCTAのLINEボタンのリンク先を本物に差し替え）
- Before/After 実例写真と実話
- 実店舗写真の撮影: 外観／入口／個室内全景／カウンセリング風景／指導中／駐車場サービス券（今回はスコープ外。将来的にAIっぽさ除去の最重要打ち手として撮影を推奨）
- `/` と `/lp` の役割分担（/lp を広告専用にするか、canonical を / に向けるか）
